const User = require("../model/user");

const handleLogin = {
  //misa's code
};

const handleLogout = {
  //misa's code
};

const verifyLoggedIn = {
  // misa's code
};

module.exports = { handleLogin, handleLogout };
